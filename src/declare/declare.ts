/**
 * @author WMXPY
 * @namespace Declare
 * @description Declare
 */

export enum CONFIG_TYPE {

    ROOT = 'ROOT',
    CHUNK = 'CHUNK',
}

export enum PROTOCOL {

    GET = 'GET',
    POST = 'POST',
}

export enum TYPE {

    STRING = 'STRING',
    INTEGER = 'INTEGER',
}
