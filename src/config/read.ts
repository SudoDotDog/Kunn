/**
 * @author WMXPY
 * @namespace Read
 * @description Read
 */

import { KunnConfig } from "../declare/kunn";

export const parseConfiguration = (config: KunnConfig): KunnConfig => {

    return config;
};
